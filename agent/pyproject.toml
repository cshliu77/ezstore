[project]
name = "ezstore-agent"
version = "0.1.0"
description = "ADK Agent for EZStore quotation management"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=1.22.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
