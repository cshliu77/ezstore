[project]
name = "ezstore-mcp-server"
version = "0.1.0"
description = "MCP Server for EZStore quotation management"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.9.0",
    "httpx>=0.28.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
